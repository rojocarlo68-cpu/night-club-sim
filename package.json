{
  "name": "night-club-sim",
  "version": "0.1.0",
  "private": true,
  "description": "Night Club — RimWorld-lite nightclub colony sim (MVP)",
  "type": "module",
  "scripts": {
    "generate:assets": "printf 'SVG placeholders ready in public/assets\\n'",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
